version: "3.5"

x-hong-kong:
  buildInfo:
    - dockerfile: "dockerfile"
      name: "rj-router"

services:
  rj-router:
    build: .
    env_file:
      - dev.env
    restart: always
    ports:
      - "9900:9900"
    networks:
      - rjnet

networks:
  rjnet:
    name: RJnet
